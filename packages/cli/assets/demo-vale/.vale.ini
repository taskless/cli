# Scoped to markdown, which is where prose rules belong. A demonstration rule
# should never widen its own scope into a project's source.
[**/*.md]
tskl) rule = prefer-use-over-utilize
prefer-use-over-utilize.prefer-use-over-utilize = YES
