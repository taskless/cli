# Scoped to READMEs, where the repository is already clean. Broadening this
# to source comments and openspec is a separate, much larger decision.
[**/README.md]
tskl) rule = no-em-dashes
BasedOnStyles =
no-em-dashes.no-em-dashes = YES

# Agent-facing instructions and the house conventions. Read as often as the
# READMEs are, by both people and agents, and small enough to keep conforming.
[CLAUDE.md]
tskl) rule = no-em-dashes
BasedOnStyles =
no-em-dashes.no-em-dashes = YES

[.conventions/*.md]
tskl) rule = no-em-dashes
BasedOnStyles =
no-em-dashes.no-em-dashes = YES

# Test fixtures are inputs to the CLI's own suite, not documentation.

# Agent-facing recipe text. Read by an agent on every authoring run, and the
# surface where a lapse propagates into generated rules, so it is held to the
# same standard as the docs a person reads.
#
# These are `.md`, so Vale parses them as markdown: fenced blocks and code
# spans are skipped, and only prose reaches this rule. They were `.txt` until
# the rename, which made every command example and identifier prose to a prose
# rule, and that is what forced whole-file exemptions rather than exemptions
# the size of the thing that earned one.
[packages/cli/src/agent/*.md]
tskl) rule = no-em-dashes
BasedOnStyles =
no-em-dashes.no-em-dashes = YES

[**/test/fixtures/**/README.md]
tskl) rule = no-em-dashes
BasedOnStyles =
no-em-dashes.no-em-dashes = NO

# Root prose docs that are not READMEs. `dotagents.md` explains the agent
# configuration to people adopting these skills elsewhere, so it is read from
# outside the team exactly as a README is, and held to the same standard.
[dotagents.md]
tskl) rule = no-em-dashes
BasedOnStyles =
no-em-dashes.no-em-dashes = YES
