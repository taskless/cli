# Notes

The config is confusing, and the missing flag is a real bug.

The version pin is what keeps the format table accurate, so leave it alone.

The second option is what we chose, for the reasons below.

PR #103 landed without a CI run, and the plane landed on time.
