// Measured on the pinned 3.18.0 binary: a bare non-comment line in bare.pyi
// yields no finding, so the file is plaintext rather than markup.
export const VALE_VERSION = "3.18.0";

// Re-probed after the bump rather than renumbered. The claim is the binary's,
// not the release notes'.
export const TIERS = [];
